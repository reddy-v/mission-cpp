
#include "gtest/gtest.h"
#include "CircularBinarySearch.h"
#include <vector>

using namespace std;

TEST(SampleCircularTest, xxxx)
{
	ASSERT_EQ(true, true);
}

TEST(SampleCircularTest, yyyy)
{
	ASSERT_EQ(true, false);
}

